@model  Pin

<head>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
          integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
          crossorigin=""/>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
            integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo="
            crossorigin=""></script>
   
</head>

<h2 class="text-center"> @Model.PinName </h2>

<div class="one" id="map" style="height: 500px"></div>

<script>
    
    var map = L.map('map').setView([@Model.PinLat, @Model.PinLong], 13);
    L.marker([@Model.PinLat,@Model.PinLong]).addTo(map);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
        maxZoom: 19
    }).addTo(map);
    </script>

<table class="table">
    <tr>
        <th> Image </th>
        <th> Journal Entry </th>
    </tr>
    <tr>
        <td> <img src="@Model.ImagePath" width="300" /></td>
        <td> @Model.PinJournal </td>
    </tr>
</table>

<div class="text-center">
    <a class="btn btn-primary" href="/Pin/UpdatePin/@Model.PinId">Update this Pin</a>
</div>

<div class="text-center">
    @using (Html.BeginForm("DeletePin", "Pin", "Post"))
    {
        <input type="hidden" asp-for="PinId" value="@Model.PinId" />
        <input class="btn btn-danger" type="submit" onclick="return confirm('Are you sure?')" value="Delete this pin" />
    }
</div>
